<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Send debug messages to System.out -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- By default, encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level  - %class - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="RP" class="com.epam.reportportal.logback.appender.ReportPortalAppender">
        <encoder>
            <!--Best practice: don't put time and logging level to the final message. Appender do this for you-->
<!--            <pattern>%d{HH:mm:ss.SSS} [%t] %-5level - %msg%n</pattern>-->
            <pattern>%d{HH:mm:ss.SSS} [%t] - %msg%n</pattern>
        </encoder>
    </appender>

    <!--'additivity' flag is important! Without it logback will double-log log messages-->
    <logger name="binary_data_logger" level="TRACE" additivity="false">
        <appender-ref ref="RP"/>
    </logger>

    <logger name="com.epam.reportportal.service" level="INFO"/>
    <logger name="com.epam.reportportal.utils" level="INFO"/>

    <!-- By default, the level of the root level is set to INFO -->
    <root level="INFO">
        <appender-ref ref="RP"/>
        <appender-ref ref="STDOUT" />
    </root>
   <logger name="report_portal" level="INFO" additivity="false">
       <appender-ref ref="RP"/>
   </logger>
    <logger name="console" level="INFO" additivity="false">
        <appender-ref ref="STDOUT"></appender-ref>
    </logger>

</configuration>